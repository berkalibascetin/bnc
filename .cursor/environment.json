{
  "name": "bnc — Freqtrade Score4WindowStrategy",
  "install": "sudo apt-get update && sudo apt-get install -y --no-install-recommends python3.12-venv python3-dev build-essential && python3 -m venv .venv && .venv/bin/python -m pip install --upgrade pip wheel setuptools && .venv/bin/python -m pip install -r requirements.txt"
}
